<div class="row odd-even-list-lightgrey">
  <div class="col-md-4 offset-md-4">
    <div class="row">
      <div class="col-6 py-2">{{ label }}</div>
      {% for item in items %}
        <div class="col-6 py-2 text-align-right">{{ item.content }}</div>
      {% endfor %}
    </div>
  </div>
</div>
